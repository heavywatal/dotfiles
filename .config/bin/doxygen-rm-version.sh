#!/bin/sh
set -eux
doxygen "$@"
OUT_DIR="${DOXYGEN_HTML_OUTPUT:-docs}"
sd '([Dd]oxygen) [\d.]+' '$1' "${OUT_DIR}"/*.html "${OUT_DIR}"/*.css
sd '^(Generated by.+doxygen.+) [\d.]+$' '$1' "${OUT_DIR}"/*.html
