#compdef rv

autoload -U is-at-least

_rv() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'*--ruby-dir=[Ruby directories to search for installations]:RUBY_DIR:_default' \
'--color=[]:COLOR:((auto\:"Use color output if the output supports it"
always\:"Force color output, even if the output isn'\''t a terminal"
never\:"Disable color output, even if the output is a compatible terminal"))' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_rv_commands" \
"*::: :->rv" \
&& ret=0
    case $state in
    (rv)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-command-$line[1]:"
        case $line[1] in
            (ruby)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
":: :_rv__subcmd__ruby_commands" \
"*::: :->ruby" \
&& ret=0

    case $state in
    (ruby)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-ruby-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format for the Ruby list]:FORMAT:(text json)' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'--all[List all versions (Including outdated)]' \
'--installed-only[List only installed versions]' \
'--no-color[By default, the table view is colored. Set this to skip coloring]' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(pin)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'--resolved[Write the resolved Ruby version instead of the request]' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'::version -- The Ruby version to pin:_default' \
&& ret=0
;;
(dir)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(find)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'::version -- Ruby version to find:_default' \
&& ret=0
;;
(install)
_arguments "${_arguments_options[@]}" : \
'-i+[Directory to install into]:DIR:_default' \
'--install-dir=[Directory to install into]:DIR:_default' \
'--tarball-path=[Path to a local ruby tarball]:TARBALL_PATH:_default' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'--force[Overwrite an existing installed version]' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'::version -- Ruby version to install:_default' \
&& ret=0
;;
(uninstall)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
':version -- Ruby version to uninstall:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__ruby__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-ruby-help-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(pin)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(dir)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(find)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(install)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(uninstall)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(cache)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
":: :_rv__subcmd__cache_commands" \
"*::: :->cache" \
&& ret=0

    case $state in
    (cache)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-cache-command-$line[1]:"
        case $line[1] in
            (clean)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(prune)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(dir)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__cache__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-cache-help-command-$line[1]:"
        case $line[1] in
            (clean)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(prune)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(dir)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(shell)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'::shell:(zsh bash fish nu powershell)' \
":: :_rv__subcmd__shell_commands" \
"*::: :->shell" \
&& ret=0

    case $state in
    (shell)
        words=($line[2] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-shell-command-$line[2]:"
        case $line[2] in
            (init)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
':shell:(zsh bash fish nu powershell)' \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
':shell:(zsh bash fish nu powershell)' \
&& ret=0
;;
(env)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
':shell:(zsh bash fish nu powershell)' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__shell__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-shell-help-command-$line[1]:"
        case $line[1] in
            (init)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(env)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(clean-install)
_arguments "${_arguments_options[@]}" : \
'--gemfile=[Path to Gemfile]:GEMFILE:_default' \
'--max-concurrent-requests=[Maximum number of downloads that can be in flight at once]:MAX_CONCURRENT_REQUESTS:_default' \
'--max-concurrent-installs=[Maximum number of gem installations that can be in flight at once. This reduces concurrently-open files on your filesystem, and concurrent disk operations]:MAX_CONCURRENT_INSTALLS:_default' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'--validate-checksums[Validate the checksums from the gem server and gem itself]' \
'--force[Force installation of gems, whatever is installed or not]' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(ci)
_arguments "${_arguments_options[@]}" : \
'--gemfile=[Path to Gemfile]:GEMFILE:_default' \
'--max-concurrent-requests=[Maximum number of downloads that can be in flight at once]:MAX_CONCURRENT_REQUESTS:_default' \
'--max-concurrent-installs=[Maximum number of gem installations that can be in flight at once. This reduces concurrently-open files on your filesystem, and concurrent disk operations]:MAX_CONCURRENT_INSTALLS:_default' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'--validate-checksums[Validate the checksums from the gem server and gem itself]' \
'--force[Force installation of gems, whatever is installed or not]' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(self)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
":: :_rv__subcmd__self_commands" \
"*::: :->self" \
&& ret=0

    case $state in
    (self)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-self-command-$line[1]:"
        case $line[1] in
            (update)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(version)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__self__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-self-help-command-$line[1]:"
        case $line[1] in
            (update)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(version)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(s)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
":: :_rv__subcmd__self_commands" \
"*::: :->self" \
&& ret=0

    case $state in
    (self)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-self-command-$line[1]:"
        case $line[1] in
            (update)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(version)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__self__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-self-help-command-$line[1]:"
        case $line[1] in
            (update)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(version)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(tool)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
":: :_rv__subcmd__tool_commands" \
"*::: :->tool" \
&& ret=0

    case $state in
    (tool)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-tool-command-$line[1]:"
        case $line[1] in
            (install)
_arguments "${_arguments_options[@]}" : \
'--gem-server=[What gem server to use]:GEM_SERVER:_default' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'-f[If true, and the tool is already installed, reinstall it. Otherwise, skip installing if the tool was already installed]' \
'--force[If true, and the tool is already installed, reinstall it. Otherwise, skip installing if the tool was already installed]' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
':gem -- What to install. This can either be gem@version, e.g. `mygem@2.18.0`, or a gem name like `mygem`, which is equivalent to doing `mygem@latest`:_default' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
'--format=[Output format for the list]:FORMAT:(text json)' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(uninstall)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
':gem -- What to uninstall:_default' \
&& ret=0
;;
(run)
_arguments "${_arguments_options[@]}" : \
'--from=[Which gem to run the executable from. If not given, assumes the gem name is the same as the executable name]:GEM:_default' \
'--gem-server=[What gem server to use, if the tool needs to be installed]:GEM_SERVER:_default' \
'*-w+[Additional gems to install alongside the primary tool]:WITH:_default' \
'*--with=[Additional gems to install alongside the primary tool]:WITH:_default' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'--no-install[By default, if the tool isn'\''t installed, rv will install it. If this flag is given, rv will exit with an error instead of installing]' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'*::args -- Command to run, e.g. `rerun` or `rails@8.0.2 new .`:_default' \
&& ret=0
;;
(dir)
_arguments "${_arguments_options[@]}" : \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__tool__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-tool-help-command-$line[1]:"
        case $line[1] in
            (install)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(uninstall)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(run)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(dir)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(run)
_arguments "${_arguments_options[@]}" : \
'--ruby=[Ruby version to use]:RUBY:_default' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'--no-install[By default, rv will install Ruby if needed. If this flag is given, rv will exit with an error instead of installing]' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'*::args -- What to run with Ruby available, e.g. `ruby myscript.rb`:_default' \
&& ret=0
;;
(r)
_arguments "${_arguments_options[@]}" : \
'--ruby=[Ruby version to use]:RUBY:_default' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'--no-install[By default, rv will install Ruby if needed. If this flag is given, rv will exit with an error instead of installing]' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'*::args -- What to run with Ruby available, e.g. `ruby myscript.rb`:_default' \
&& ret=0
;;
(fmt)
_arguments "${_arguments_options[@]}" : \
'()--stdin-filepath=[When reading from stdin, treat the input as if it were at this path. This allows .rubyfmtignore and .gitignore patterns to be applied to stdin input]:stdin-filepath:_default' \
'--cache-dir=[Path to the cache directory]:CACHE_DIR:_default' \
'-c[Turn on check mode. This outputs diffs of inputs to STDOUT. Will exit non-zero when differences are detected]' \
'--check[Turn on check mode. This outputs diffs of inputs to STDOUT. Will exit non-zero when differences are detected]' \
'--include-gitignored[Turn on to format gitignored files. Gitignored files are ignored by default]' \
'--header-opt-in[Only format ruby files containing the magic \`# rubyfmt\: true\` header]' \
'--header-opt-out[Do not format ruby files containing the magic \`# rubyfmt\: false\` header]' \
'--fail-fast[Fail on all syntax and io errors early. Warnings otherwise]' \
'-s[Write formatted files to STDOUT, instead of formatting in place]' \
'--stdout[Write formatted files to STDOUT, instead of formatting in place]' \
'--is-tty[If stdin is attached to a terminal]' \
'*-v[Increase logging verbosity]' \
'*--verbose[Increase logging verbosity]' \
'(-v --verbose)*-q[Decrease logging verbosity]' \
'(-v --verbose)*--quiet[Decrease logging verbosity]' \
'-h[]' \
'--help[]' \
'--offline[Run rv in offline mode if possible TODO\: Hide until really apply offline mode in all parts of rv]' \
'-n[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'--no-cache[Avoid reading from or writing to the cache, instead using a temporary directory for the duration of the operation]' \
'*::paths -- Paths to format. To format the entire project, run `rv fmt .`{n} Acceptable paths are\:{n} - File paths (i.e lib/foo/bar.rb){n} - Directories (i.e. lib/foo/){n} - Input files (i.e. @/tmp/files.txt). These files must contain one file path or directory per line:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-help-command-$line[1]:"
        case $line[1] in
            (ruby)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__help__subcmd__ruby_commands" \
"*::: :->ruby" \
&& ret=0

    case $state in
    (ruby)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-help-ruby-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(pin)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(dir)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(find)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(install)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(uninstall)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(cache)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__help__subcmd__cache_commands" \
"*::: :->cache" \
&& ret=0

    case $state in
    (cache)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-help-cache-command-$line[1]:"
        case $line[1] in
            (clean)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(prune)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(dir)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(shell)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__help__subcmd__shell_commands" \
"*::: :->shell" \
&& ret=0

    case $state in
    (shell)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-help-shell-command-$line[1]:"
        case $line[1] in
            (init)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(env)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(clean-install)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(self)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__help__subcmd__self_commands" \
"*::: :->self" \
&& ret=0

    case $state in
    (self)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-help-self-command-$line[1]:"
        case $line[1] in
            (update)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(version)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(tool)
_arguments "${_arguments_options[@]}" : \
":: :_rv__subcmd__help__subcmd__tool_commands" \
"*::: :->tool" \
&& ret=0

    case $state in
    (tool)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:rv-help-tool-command-$line[1]:"
        case $line[1] in
            (install)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(uninstall)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(run)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(dir)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(run)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(fmt)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_rv_commands] )) ||
_rv_commands() {
    local commands; commands=(
'ruby:Manage Ruby versions and installations' \
'cache:Manage rv'\''s cache' \
'shell:Configure your shell to use rv' \
'clean-install:Clean install from a Gemfile.lock' \
'ci:Clean install from a Gemfile.lock' \
'self:Manage rv itself' \
's:Manage rv itself' \
'tool:Manage Ruby tools' \
'run:Run a command or script with Ruby' \
'r:Run a command or script with Ruby' \
'fmt:Format Ruby files consistently' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv commands' commands "$@"
}
(( $+functions[_rv__subcmd__cache_commands] )) ||
_rv__subcmd__cache_commands() {
    local commands; commands=(
'clean:Clear the cache' \
'prune:Prune all unused entries from the cache' \
'dir:Show the cache directory' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv cache commands' commands "$@"
}
(( $+functions[_rv__subcmd__cache__subcmd__clean_commands] )) ||
_rv__subcmd__cache__subcmd__clean_commands() {
    local commands; commands=()
    _describe -t commands 'rv cache clean commands' commands "$@"
}
(( $+functions[_rv__subcmd__cache__subcmd__dir_commands] )) ||
_rv__subcmd__cache__subcmd__dir_commands() {
    local commands; commands=()
    _describe -t commands 'rv cache dir commands' commands "$@"
}
(( $+functions[_rv__subcmd__cache__subcmd__help_commands] )) ||
_rv__subcmd__cache__subcmd__help_commands() {
    local commands; commands=(
'clean:Clear the cache' \
'prune:Prune all unused entries from the cache' \
'dir:Show the cache directory' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv cache help commands' commands "$@"
}
(( $+functions[_rv__subcmd__cache__subcmd__help__subcmd__clean_commands] )) ||
_rv__subcmd__cache__subcmd__help__subcmd__clean_commands() {
    local commands; commands=()
    _describe -t commands 'rv cache help clean commands' commands "$@"
}
(( $+functions[_rv__subcmd__cache__subcmd__help__subcmd__dir_commands] )) ||
_rv__subcmd__cache__subcmd__help__subcmd__dir_commands() {
    local commands; commands=()
    _describe -t commands 'rv cache help dir commands' commands "$@"
}
(( $+functions[_rv__subcmd__cache__subcmd__help__subcmd__help_commands] )) ||
_rv__subcmd__cache__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'rv cache help help commands' commands "$@"
}
(( $+functions[_rv__subcmd__cache__subcmd__help__subcmd__prune_commands] )) ||
_rv__subcmd__cache__subcmd__help__subcmd__prune_commands() {
    local commands; commands=()
    _describe -t commands 'rv cache help prune commands' commands "$@"
}
(( $+functions[_rv__subcmd__cache__subcmd__prune_commands] )) ||
_rv__subcmd__cache__subcmd__prune_commands() {
    local commands; commands=()
    _describe -t commands 'rv cache prune commands' commands "$@"
}
(( $+functions[_rv__subcmd__clean-install_commands] )) ||
_rv__subcmd__clean-install_commands() {
    local commands; commands=()
    _describe -t commands 'rv clean-install commands' commands "$@"
}
(( $+functions[_rv__subcmd__fmt_commands] )) ||
_rv__subcmd__fmt_commands() {
    local commands; commands=()
    _describe -t commands 'rv fmt commands' commands "$@"
}
(( $+functions[_rv__subcmd__help_commands] )) ||
_rv__subcmd__help_commands() {
    local commands; commands=(
'ruby:Manage Ruby versions and installations' \
'cache:Manage rv'\''s cache' \
'shell:Configure your shell to use rv' \
'clean-install:Clean install from a Gemfile.lock' \
'self:Manage rv itself' \
'tool:Manage Ruby tools' \
'run:Run a command or script with Ruby' \
'fmt:Format Ruby files consistently' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv help commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__cache_commands] )) ||
_rv__subcmd__help__subcmd__cache_commands() {
    local commands; commands=(
'clean:Clear the cache' \
'prune:Prune all unused entries from the cache' \
'dir:Show the cache directory' \
    )
    _describe -t commands 'rv help cache commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__cache__subcmd__clean_commands] )) ||
_rv__subcmd__help__subcmd__cache__subcmd__clean_commands() {
    local commands; commands=()
    _describe -t commands 'rv help cache clean commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__cache__subcmd__dir_commands] )) ||
_rv__subcmd__help__subcmd__cache__subcmd__dir_commands() {
    local commands; commands=()
    _describe -t commands 'rv help cache dir commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__cache__subcmd__prune_commands] )) ||
_rv__subcmd__help__subcmd__cache__subcmd__prune_commands() {
    local commands; commands=()
    _describe -t commands 'rv help cache prune commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__clean-install_commands] )) ||
_rv__subcmd__help__subcmd__clean-install_commands() {
    local commands; commands=()
    _describe -t commands 'rv help clean-install commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__fmt_commands] )) ||
_rv__subcmd__help__subcmd__fmt_commands() {
    local commands; commands=()
    _describe -t commands 'rv help fmt commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__help_commands] )) ||
_rv__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'rv help help commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__ruby_commands] )) ||
_rv__subcmd__help__subcmd__ruby_commands() {
    local commands; commands=(
'list:List all installed and available Ruby versions' \
'pin:Show or set the Ruby version for the current project' \
'dir:Show the directory where all Ruby versions are installed' \
'find:Show the path to the Ruby executable for the pinned version or a specific version' \
'install:Install Ruby' \
'uninstall:Uninstall a specific Ruby version' \
    )
    _describe -t commands 'rv help ruby commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__ruby__subcmd__dir_commands] )) ||
_rv__subcmd__help__subcmd__ruby__subcmd__dir_commands() {
    local commands; commands=()
    _describe -t commands 'rv help ruby dir commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__ruby__subcmd__find_commands] )) ||
_rv__subcmd__help__subcmd__ruby__subcmd__find_commands() {
    local commands; commands=()
    _describe -t commands 'rv help ruby find commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__ruby__subcmd__install_commands] )) ||
_rv__subcmd__help__subcmd__ruby__subcmd__install_commands() {
    local commands; commands=()
    _describe -t commands 'rv help ruby install commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__ruby__subcmd__list_commands] )) ||
_rv__subcmd__help__subcmd__ruby__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'rv help ruby list commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__ruby__subcmd__pin_commands] )) ||
_rv__subcmd__help__subcmd__ruby__subcmd__pin_commands() {
    local commands; commands=()
    _describe -t commands 'rv help ruby pin commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__ruby__subcmd__uninstall_commands] )) ||
_rv__subcmd__help__subcmd__ruby__subcmd__uninstall_commands() {
    local commands; commands=()
    _describe -t commands 'rv help ruby uninstall commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__run_commands] )) ||
_rv__subcmd__help__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'rv help run commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__self_commands] )) ||
_rv__subcmd__help__subcmd__self_commands() {
    local commands; commands=(
'update:Update rv to the latest version' \
'version:Display rv'\''s version' \
    )
    _describe -t commands 'rv help self commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__self__subcmd__update_commands] )) ||
_rv__subcmd__help__subcmd__self__subcmd__update_commands() {
    local commands; commands=()
    _describe -t commands 'rv help self update commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__self__subcmd__version_commands] )) ||
_rv__subcmd__help__subcmd__self__subcmd__version_commands() {
    local commands; commands=()
    _describe -t commands 'rv help self version commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__shell_commands] )) ||
_rv__subcmd__help__subcmd__shell_commands() {
    local commands; commands=(
'init:' \
'completions:' \
'env:' \
    )
    _describe -t commands 'rv help shell commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__shell__subcmd__completions_commands] )) ||
_rv__subcmd__help__subcmd__shell__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'rv help shell completions commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__shell__subcmd__env_commands] )) ||
_rv__subcmd__help__subcmd__shell__subcmd__env_commands() {
    local commands; commands=()
    _describe -t commands 'rv help shell env commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__shell__subcmd__init_commands] )) ||
_rv__subcmd__help__subcmd__shell__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'rv help shell init commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__tool_commands] )) ||
_rv__subcmd__help__subcmd__tool_commands() {
    local commands; commands=(
'install:Install a gem as a CLI tool, with its own dedicated environment' \
'list:List installed tools' \
'uninstall:Remove an installed tool' \
'run:Run a command from a gem, installing it if necessary' \
'dir:Show the path to the rv tools directory' \
    )
    _describe -t commands 'rv help tool commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__tool__subcmd__dir_commands] )) ||
_rv__subcmd__help__subcmd__tool__subcmd__dir_commands() {
    local commands; commands=()
    _describe -t commands 'rv help tool dir commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__tool__subcmd__install_commands] )) ||
_rv__subcmd__help__subcmd__tool__subcmd__install_commands() {
    local commands; commands=()
    _describe -t commands 'rv help tool install commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__tool__subcmd__list_commands] )) ||
_rv__subcmd__help__subcmd__tool__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'rv help tool list commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__tool__subcmd__run_commands] )) ||
_rv__subcmd__help__subcmd__tool__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'rv help tool run commands' commands "$@"
}
(( $+functions[_rv__subcmd__help__subcmd__tool__subcmd__uninstall_commands] )) ||
_rv__subcmd__help__subcmd__tool__subcmd__uninstall_commands() {
    local commands; commands=()
    _describe -t commands 'rv help tool uninstall commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby_commands] )) ||
_rv__subcmd__ruby_commands() {
    local commands; commands=(
'list:List all installed and available Ruby versions' \
'pin:Show or set the Ruby version for the current project' \
'dir:Show the directory where all Ruby versions are installed' \
'find:Show the path to the Ruby executable for the pinned version or a specific version' \
'install:Install Ruby' \
'uninstall:Uninstall a specific Ruby version' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv ruby commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__dir_commands] )) ||
_rv__subcmd__ruby__subcmd__dir_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby dir commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__find_commands] )) ||
_rv__subcmd__ruby__subcmd__find_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby find commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__help_commands] )) ||
_rv__subcmd__ruby__subcmd__help_commands() {
    local commands; commands=(
'list:List all installed and available Ruby versions' \
'pin:Show or set the Ruby version for the current project' \
'dir:Show the directory where all Ruby versions are installed' \
'find:Show the path to the Ruby executable for the pinned version or a specific version' \
'install:Install Ruby' \
'uninstall:Uninstall a specific Ruby version' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv ruby help commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__help__subcmd__dir_commands] )) ||
_rv__subcmd__ruby__subcmd__help__subcmd__dir_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby help dir commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__help__subcmd__find_commands] )) ||
_rv__subcmd__ruby__subcmd__help__subcmd__find_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby help find commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__help__subcmd__help_commands] )) ||
_rv__subcmd__ruby__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby help help commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__help__subcmd__install_commands] )) ||
_rv__subcmd__ruby__subcmd__help__subcmd__install_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby help install commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__help__subcmd__list_commands] )) ||
_rv__subcmd__ruby__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby help list commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__help__subcmd__pin_commands] )) ||
_rv__subcmd__ruby__subcmd__help__subcmd__pin_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby help pin commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__help__subcmd__uninstall_commands] )) ||
_rv__subcmd__ruby__subcmd__help__subcmd__uninstall_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby help uninstall commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__install_commands] )) ||
_rv__subcmd__ruby__subcmd__install_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby install commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__list_commands] )) ||
_rv__subcmd__ruby__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby list commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__pin_commands] )) ||
_rv__subcmd__ruby__subcmd__pin_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby pin commands' commands "$@"
}
(( $+functions[_rv__subcmd__ruby__subcmd__uninstall_commands] )) ||
_rv__subcmd__ruby__subcmd__uninstall_commands() {
    local commands; commands=()
    _describe -t commands 'rv ruby uninstall commands' commands "$@"
}
(( $+functions[_rv__subcmd__run_commands] )) ||
_rv__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'rv run commands' commands "$@"
}
(( $+functions[_rv__subcmd__self_commands] )) ||
_rv__subcmd__self_commands() {
    local commands; commands=(
'update:Update rv to the latest version' \
'version:Display rv'\''s version' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv self commands' commands "$@"
}
(( $+functions[_rv__subcmd__self__subcmd__help_commands] )) ||
_rv__subcmd__self__subcmd__help_commands() {
    local commands; commands=(
'update:Update rv to the latest version' \
'version:Display rv'\''s version' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv self help commands' commands "$@"
}
(( $+functions[_rv__subcmd__self__subcmd__help__subcmd__help_commands] )) ||
_rv__subcmd__self__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'rv self help help commands' commands "$@"
}
(( $+functions[_rv__subcmd__self__subcmd__help__subcmd__update_commands] )) ||
_rv__subcmd__self__subcmd__help__subcmd__update_commands() {
    local commands; commands=()
    _describe -t commands 'rv self help update commands' commands "$@"
}
(( $+functions[_rv__subcmd__self__subcmd__help__subcmd__version_commands] )) ||
_rv__subcmd__self__subcmd__help__subcmd__version_commands() {
    local commands; commands=()
    _describe -t commands 'rv self help version commands' commands "$@"
}
(( $+functions[_rv__subcmd__self__subcmd__update_commands] )) ||
_rv__subcmd__self__subcmd__update_commands() {
    local commands; commands=()
    _describe -t commands 'rv self update commands' commands "$@"
}
(( $+functions[_rv__subcmd__self__subcmd__version_commands] )) ||
_rv__subcmd__self__subcmd__version_commands() {
    local commands; commands=()
    _describe -t commands 'rv self version commands' commands "$@"
}
(( $+functions[_rv__subcmd__shell_commands] )) ||
_rv__subcmd__shell_commands() {
    local commands; commands=(
'init:' \
'completions:' \
'env:' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv shell commands' commands "$@"
}
(( $+functions[_rv__subcmd__shell__subcmd__completions_commands] )) ||
_rv__subcmd__shell__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'rv shell completions commands' commands "$@"
}
(( $+functions[_rv__subcmd__shell__subcmd__env_commands] )) ||
_rv__subcmd__shell__subcmd__env_commands() {
    local commands; commands=()
    _describe -t commands 'rv shell env commands' commands "$@"
}
(( $+functions[_rv__subcmd__shell__subcmd__help_commands] )) ||
_rv__subcmd__shell__subcmd__help_commands() {
    local commands; commands=(
'init:' \
'completions:' \
'env:' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv shell help commands' commands "$@"
}
(( $+functions[_rv__subcmd__shell__subcmd__help__subcmd__completions_commands] )) ||
_rv__subcmd__shell__subcmd__help__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'rv shell help completions commands' commands "$@"
}
(( $+functions[_rv__subcmd__shell__subcmd__help__subcmd__env_commands] )) ||
_rv__subcmd__shell__subcmd__help__subcmd__env_commands() {
    local commands; commands=()
    _describe -t commands 'rv shell help env commands' commands "$@"
}
(( $+functions[_rv__subcmd__shell__subcmd__help__subcmd__help_commands] )) ||
_rv__subcmd__shell__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'rv shell help help commands' commands "$@"
}
(( $+functions[_rv__subcmd__shell__subcmd__help__subcmd__init_commands] )) ||
_rv__subcmd__shell__subcmd__help__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'rv shell help init commands' commands "$@"
}
(( $+functions[_rv__subcmd__shell__subcmd__init_commands] )) ||
_rv__subcmd__shell__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'rv shell init commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool_commands] )) ||
_rv__subcmd__tool_commands() {
    local commands; commands=(
'install:Install a gem as a CLI tool, with its own dedicated environment' \
'list:List installed tools' \
'uninstall:Remove an installed tool' \
'run:Run a command from a gem, installing it if necessary' \
'dir:Show the path to the rv tools directory' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv tool commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__dir_commands] )) ||
_rv__subcmd__tool__subcmd__dir_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool dir commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__help_commands] )) ||
_rv__subcmd__tool__subcmd__help_commands() {
    local commands; commands=(
'install:Install a gem as a CLI tool, with its own dedicated environment' \
'list:List installed tools' \
'uninstall:Remove an installed tool' \
'run:Run a command from a gem, installing it if necessary' \
'dir:Show the path to the rv tools directory' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'rv tool help commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__help__subcmd__dir_commands] )) ||
_rv__subcmd__tool__subcmd__help__subcmd__dir_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool help dir commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__help__subcmd__help_commands] )) ||
_rv__subcmd__tool__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool help help commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__help__subcmd__install_commands] )) ||
_rv__subcmd__tool__subcmd__help__subcmd__install_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool help install commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__help__subcmd__list_commands] )) ||
_rv__subcmd__tool__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool help list commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__help__subcmd__run_commands] )) ||
_rv__subcmd__tool__subcmd__help__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool help run commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__help__subcmd__uninstall_commands] )) ||
_rv__subcmd__tool__subcmd__help__subcmd__uninstall_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool help uninstall commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__install_commands] )) ||
_rv__subcmd__tool__subcmd__install_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool install commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__list_commands] )) ||
_rv__subcmd__tool__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool list commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__run_commands] )) ||
_rv__subcmd__tool__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool run commands' commands "$@"
}
(( $+functions[_rv__subcmd__tool__subcmd__uninstall_commands] )) ||
_rv__subcmd__tool__subcmd__uninstall_commands() {
    local commands; commands=()
    _describe -t commands 'rv tool uninstall commands' commands "$@"
}

if [ "$funcstack[1]" = "_rv" ]; then
    _rv "$@"
else
    compdef _rv rv
fi
